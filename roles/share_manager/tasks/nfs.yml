---
- name: Ensure NFS Export Exists
  ansible.builtin.lineinfile:
    path: /etc/exports
    line: "{{ item.path }} {{ share_manager_nfs_default_options }}"
    state: present
  loop: "{{ share_manager_shares_normalized }}"
  notify: Restart NFS
